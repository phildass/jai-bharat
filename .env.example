# Jai Bharat – Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to source control.

# ── API Configuration ─────────────────────────────────────────────────────────
# Base URL for the Jai Bharat backend API (used by the React Native app and web)
API_BASE_URL=https://api.jaibharat.cloud
# For local development: http://localhost:8080

# ── Backend Server ────────────────────────────────────────────────────────────
PORT=8080
NODE_ENV=development

# Enable in-memory test data (no DB required) – set to true for testing without a database
TEST_DATA_MODE=false

# Comma-separated extra CORS origins (beyond the built-in defaults)
# CORS_ORIGINS=https://bharat.jaibharat.cloud,http://72.60.203.189:3001

# ── Database ──────────────────────────────────────────────────────────────────
DATABASE_URL=postgresql://user:password@localhost:5432/jai_bharat

# ── Supabase ──────────────────────────────────────────────────────────────────
# Project URL and anon key (safe for client/browser)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
# Service role key – NEVER expose to browser or mobile bundle
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# ── Next.js Web App (apps/web) ────────────────────────────────────────────────
# Prefix NEXT_PUBLIC_ makes these available in the browser bundle (safe values only)
NEXT_PUBLIC_API_BASE_URL=https://api.jaibharat.cloud
# For local development: http://localhost:8080

# APK download URL shown on the "Download App" button in the header
NEXT_PUBLIC_APP_DOWNLOAD_URL=https://download.jaibharat.cloud/jai-bharat.apk

# ── Payment Configuration ─────────────────────────────────────────────────────
PAYMENT_URL=https://aienter.in/payments/jaibharatpay
PAYMENT_AMOUNT=116.82

# ── Webhook Security ──────────────────────────────────────────────────────────
# Secret key used to verify incoming webhooks from aienter.in
AIENTER_WEBHOOK_SECRET=your-webhook-secret-key

# ── Support ───────────────────────────────────────────────────────────────────
SUPPORT_EMAIL=support@jaibharat.cloud

# ── LocationIQ (Jobs Near Me – reverse geocoding) ─────────────────────────────
# Obtain a free key at https://locationiq.com
# The key is NEVER sent to the client; all calls go through /api/geo/reverse
LOCATIONIQ_API_KEY=your-locationiq-api-key
