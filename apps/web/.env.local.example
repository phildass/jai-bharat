# apps/web – Next.js environment variables
# Copy to .env.local for local development. NEVER commit .env.local.

# Backend API base URL (safe to expose – no secrets)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Production (set in hosting platform, e.g., Vercel / Railway):
# NEXT_PUBLIC_API_BASE_URL=https://api.jaibharat.cloud

# APK download URL (shown on "Download App" button in the header).
# - Set to a real APK URL to enable APK download (e.g. https://cdn.example.com/jai-bharat.apk).
# - Leave as the placeholder path (/downloads/jai-bharat.apk) to show "App Coming Soon".
# - If NEXT_PUBLIC_ENABLE_PWA=true the header will show a PWA "Install App" button instead
#   (powered by the beforeinstallprompt browser event) and this URL is ignored for eligible browsers.
NEXT_PUBLIC_APP_DOWNLOAD_URL=/downloads/jai-bharat.apk

# Set to true to enable PWA install-prompt mode in the header.
# When enabled:
#   - If the browser fires beforeinstallprompt, an "Install App" button is shown.
#   - After install (appinstalled event) or when running in standalone mode, the button is hidden.
#   - If beforeinstallprompt does not fire (e.g. iOS Safari), falls back to the APK/coming-soon link.
# When false (default), only the APK download link is shown.
NEXT_PUBLIC_ENABLE_PWA=true

# Payment URL (shown on "Pay Now" button on the Jobs page)
NEXT_PUBLIC_PAYMENT_URL=https://aienter.in/payments/jaibharat
